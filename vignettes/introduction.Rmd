---
title: "introduction"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{introduction}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
	echo = FALSE,
	fig.align="center",
	fig.show="hold",
  comment = "#>"
)
```

Le *package* `apimisc` permet d'utiliser plusieurs API (assez) facilement.

Les fonctions sont divisées en plusieurs catégories :

+ `apimisc_xxx` : fonctions diverses (génériques)

+ `bdf_xxx` : pour l'API Banque de France

+ `imf_xxx` : pour l'API du FMI

+ `if_xxx` : pour l'API Info-financieres

+ `wb_xxx` : pour l'API World Bank

